# Express Checkout demo — public merchant-application token used to init the express element.
# The backend still validates clientId server-side; this is not a secret.
VITE_EXPRESS_CLIENT_ID=

# Optional local wallet-testing knobs (see README "Express Checkout demo"):
# EXPRESS_HTTPS=true
# OPG_PROXY_TARGET=https://api.checkout.integration.oscato.com
